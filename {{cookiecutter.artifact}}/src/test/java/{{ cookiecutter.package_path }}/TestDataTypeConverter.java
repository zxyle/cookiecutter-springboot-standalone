// This project was generated by {{ cookiecutter.author_name }} at {% now 'local', '%Y-%m-%d %X' %}.
// The version of generator is v{{ cookiecutter._version }}.

package {{ cookiecutter.basePackage }};

import {{ cookiecutter.basePackage }}.common.util.DataTypeConverter;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

/**
 * 数据类型转换工具类单元测试
 */
class TestDataTypeConverter {

    @Test
    void test01() {
        String strValue = DataTypeConverter.convert("aaa", "java.lang.String");
        int intValue = DataTypeConverter.convert("111", "java.lang.Integer");
        Assertions.assertEquals("aaa", strValue);
        Assertions.assertEquals(intValue, 111);
    }
}
