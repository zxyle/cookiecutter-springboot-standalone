// This project was generated by {{ cookiecutter.author_name }} at {% now 'local', '%Y-%m-%d %X' %}.
// The version of generator is v{{ cookiecutter._version }}.

package {{ cookiecutter.basePackage }}.biz.auth.request.app;

import lombok.Data;
import org.hibernate.validator.constraints.Length;
import org.hibernate.validator.constraints.URL;

/**
 * 更新应用请求
 */
@Data
public class UpdateAppRequest {

    /**
     * 应用中文名称
     *
     * @mock 风险预警系统
     */
    @Length(max = 32, message = "应用名称长度不能超过32个字符")
    private String name;

    /**
     * 应用描述
     *
     * @mock 风险预警系统
     */
    @Length(max = 255, message = "应用描述长度不能超过255个字符")
    private String description;

    /**
     * 回调地址
     */
    @URL(message = "回调地址格式不正确")
    @Length(max = 255, message = "回调地址长度不能超过255个字符")
    private String redirectUrl;

    /**
     * 应用logo
     */
    @Length(max = 255, message = "应用logo长度不能超过255个字符")
    private String logo;
}
