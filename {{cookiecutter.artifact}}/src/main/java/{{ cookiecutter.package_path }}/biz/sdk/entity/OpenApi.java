// This project was generated by {{ cookiecutter.author_name }} at {% now 'local', '%Y-%m-%d %X' %}.
// The version of generator is v{{ cookiecutter._version }}.

package {{ cookiecutter.basePackage }}.biz.sdk.entity;

import com.baomidou.mybatisplus.annotation.TableName;
import {{ cookiecutter.basePackage }}.common.entity.BaseEntity;
import lombok.Data;
import lombok.EqualsAndHashCode;
import org.hibernate.validator.constraints.Length;

import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotNull;
import java.time.LocalDateTime;

/**
 * 开放接口
 */
@Data
@EqualsAndHashCode(callSuper = true)
@TableName("auth_open_api")
public class OpenApi extends BaseEntity {
    private static final long serialVersionUID = 1L;

    /**
     * 应用ID
     */
    @Length(max = 32, message = "应用ID长度不能超过32个字符")
    private String appId;

    /**
     * 应用秘钥
     */
    @Length(max = 32, message = "应用秘钥长度不能超过32个字符")
    private String appSecret;

    /**
     * 过期时间
     */
    private LocalDateTime expireTime;

    /**
     * 备注
     */
    @Length(max = 32, message = "备注长度不能超过32个字符")
    @NotBlank(message = "备注不能为空")
    private String remark;

    /**
     * 用户ID
     */
    @NotNull
    private Long userId;

}
