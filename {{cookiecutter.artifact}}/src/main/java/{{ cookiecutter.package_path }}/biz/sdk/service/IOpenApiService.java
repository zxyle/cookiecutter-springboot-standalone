// This project was generated by {{ cookiecutter.author_name }} at {% now 'local', '%Y-%m-%d %X' %}.
// The version of generator is v{{ cookiecutter._version }}.

package {{ cookiecutter.basePackage }}.biz.sdk.service;

import com.baomidou.mybatisplus.core.conditions.query.QueryWrapper;
import com.baomidou.mybatisplus.core.metadata.IPage;
import com.baomidou.mybatisplus.extension.service.IService;
import {{ cookiecutter.basePackage }}.biz.sdk.entity.OpenApi;

/**
 * 开放接口 服务类
 */
public interface IOpenApiService extends IService<OpenApi> {

    /**
     * 按ID查询（带缓存）
     */
    OpenApi queryById(Long id);

    /**
     * 按ID更新（带缓存）
     */
    OpenApi putById(OpenApi entity);

    /**
     * 按ID删除（带缓存）
     */
    void deleteById(Long id);

    /**
     * 分页查询（带缓存）
     */
    IPage<OpenApi> pageQuery(IPage<OpenApi> page, QueryWrapper<OpenApi> wrapper);

    /**
     * 根据appId获取有效的OpenApi
     *
     * @param appId 应用ID
     */
    OpenApi getValidOpenApi(String appId);
}
