// This project was generated by {{ cookiecutter.author_name }} at {% now 'local', '%Y-%m-%d %X' %}.
// And git commit hash is {% gitcommit %}.

package {{ cookiecutter.basePackage }}.biz.site.legal;

import {{ cookiecutter.basePackage }}.biz.site.info.Info;
import {{ cookiecutter.basePackage }}.biz.site.info.InfoService;
import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * 法律声明管理
 */
@RestController
@RequestMapping("/site/legal")
@RequiredArgsConstructor
public class LegalController {

    final InfoService infoService;

    /**
     * 用户协议
     */
    @GetMapping(value = "/user-agreement", produces = "text/html; charset=UTF-8")
    public String getUserAgreement() {
        Info one = infoService.getByKey("user-agreement");
        return one.getParamValue();
    }

    /**
     * 隐私政策
     */
    @GetMapping(value = "/privacy-policy", produces = "text/html; charset=UTF-8")
    public String getPrivacyPolicy() {
        Info one = infoService.getByKey("privacy-policy");
        return one.getParamValue();
    }
}
