<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="{{ cookiecutter.basePackage }}.biz.sys.mapper.ParamMapper">


    <select id="selectAll" resultType="{{ cookiecutter.basePackage }}.biz.sys.entity.Param">
        SELECT * FROM sys_param
    </select>

    <update id="truncate">
        TRUNCATE TABLE sys_param
    </update>

</mapper>
