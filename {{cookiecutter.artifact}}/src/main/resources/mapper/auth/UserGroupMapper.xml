<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="{{ cookiecutter.basePackage }}.biz.auth.mapper.UserGroupMapper">

    <select id="listUsers" resultType="{{ cookiecutter.basePackage }}.biz.auth.entity.User">
        SELECT
            b.*
        FROM `auth_user_group` a
        LEFT JOIN `auth_user` b
        ON a.user_id = b.id
        WHERE a.group_id = #{groupId}
    </select>

    <select id="listGroups" resultType="{{ cookiecutter.basePackage }}.biz.auth.entity.Group">
        SELECT
            b.`name`,
            b.`parent_id`,
            b.`description`,
            b.`sort`
        FROM
            `auth_user_group` a
        LEFT JOIN `auth_group` b
            ON a.`group_id` = b.`id`
        WHERE
            a.`user_id` = 1
    </select>

</mapper>
